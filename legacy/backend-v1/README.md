# Legacy Backend Archive

This folder preserves the previous Skyrunner backend implementation before the reset architecture rollout.

Archived contents include:

- previous `src` modular backend
- Prisma schema and migrations
- tests and helper scripts
- prior deployment configuration

Do not add new implementation work here. New backend development now lives in the root `src` and `drizzle` directories.
